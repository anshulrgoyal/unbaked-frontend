import { AppaerDirective } from './appaer.directive';

describe('AppaerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppaerDirective();
    expect(directive).toBeTruthy();
  });
});
